import React from 'react';
import { Outlet } from 'react-router-dom';

const Protocols = () => {
    return (
        <div >
            Protocols
            <Outlet />
        </div>
    );
}

export default Protocols;
